import moment from 'moment';

const cellArrayReducer = (state = [], action) => {
  const { type } = action;
  switch (type) {
    // case value:

    //   break;

    default:
      return state;
  }
};
export default cellArrayReducer;
